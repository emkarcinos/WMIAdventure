import styled from 'styled-components';

const ImageInput = styled.input`
  align-self: flex-end;
  position: absolute;
  width: 100%;
  height: 100%;
  cursor: pointer;
  opacity: 0;
`;

export default ImageInput;