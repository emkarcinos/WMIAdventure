import styled from 'styled-components';

const Icon = styled.img`
  min-width: 12px;
  min-height: 12px;
  width: auto;
  height: auto;
  max-width: 16px;
  max-height: 16px;
`;

export default Icon;