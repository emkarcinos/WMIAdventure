import colors from './colors';

const theme = {
    colors,
    weight: {
        light: 300,
        regular: 400,
        semibold: 600,
        bold: 700,
    }
};

export default theme;