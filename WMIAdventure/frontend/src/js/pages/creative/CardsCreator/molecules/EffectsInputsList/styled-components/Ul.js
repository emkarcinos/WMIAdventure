import styled from 'styled-components';

const Ul = styled.ul`
  padding: 10px 0;
  margin: 0;
  list-style: none;

  grid-template-columns: auto;
  grid-row-gap: 10px;
`;

export default Ul;