import styled from 'styled-components';

const Ul = styled.ul`
  padding: 10px 0;
  margin: 0;
  list-style: none;

  display: grid;
  grid-template-columns: auto;
  grid-column-gap: 10px;
`;

export default Ul;