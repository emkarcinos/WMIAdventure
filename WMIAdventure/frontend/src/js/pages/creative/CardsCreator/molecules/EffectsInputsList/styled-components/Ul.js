import styled from 'styled-components';

const Ul = styled.ul`
  padding: 10px 0;
  margin: 0;
  list-style: none;

  flex-direction: column;
  justify-content: center;
  align-items: center;
`;

export default Ul;